# Dependencies for scripts/audio_server.py
# Install with: pip install -r scripts/requirements-audio.txt
#
# Note: torch is NOT pinned here — install the right build for your system
# first (CUDA vs CPU). See https://pytorch.org/get-started/locally/
# Example (CUDA 12.1):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

audiocraft>=1.3.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
