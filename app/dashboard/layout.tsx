export const metadata = {
  title: "LLM Dashboard — RnR Vibe (Local Only)",
  robots: "noindex, nofollow",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
